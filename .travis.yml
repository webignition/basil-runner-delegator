language: php
php:
  - "7.4"

addons:
  chrome: stable

install:
  - composer setup-dev
  - cd vendor/symfony/panther/chromedriver-bin && ./update.sh && cd ../../../..

script:
  - composer ci

cache:
  directories:
    - $HOME/.composer/cache/files
