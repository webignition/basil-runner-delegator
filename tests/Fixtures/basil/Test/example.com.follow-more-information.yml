config:
  browser: chrome
  url: https://example.com/

"follow more information":
#  actions:
#    - click $"a"

  assertions:
    - $page.url is "foo"
    - $"a" is "More foo ..."
#    - $page.url is "https://www.iana.org/domains/reserved"
