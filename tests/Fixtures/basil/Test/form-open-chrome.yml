config:
  browsers:
    - chrome
  url: $form_page.url

imports:
  pages:
    form_page: "../Page/form.yml"

"verify page is open":
  assertions:
    - $page.url is "http://nginx/form.html"
    - $page.title is "Form"
