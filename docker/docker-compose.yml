version: "3"

services:
  basil-runner:
    environment:
      - PANTHER_NO_SANDBOX=1
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    container_name: basil-runner
    tty: true
    stdin_open: true
    volumes:
      - ../tests/Fixtures/basil:/app/basil
